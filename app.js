const gameBoard = {}

function createColumn() {
    return new Map() 
}

let userBoardSize = null

for (let i = 0; i < userBoardSize; i ++){
gameBoard.set(`column` + `${i}`, createColumn)
}


function gameToken () {
 // set gameBoard.columnX.value = token 
}

function updateBoard() {
    // update board values
}